//Iport our library as a dependcy is it fails assume it is a local project

int main()
{
    //create pipeline

    //add stages

    //create loader

    //create landmarks?

    //create classifier

    //train model and check the accuracy

    //connect to mediapipe which is connected to camera

    //get the landmark


    //in original code runs pipeline again?

    //while true get the frame from the camera in orig, in our code
    //get the mediapipe connection

    //get index of the best hand

    //selest the best hand

    //predict the class of the gesture

    //peform action
    
}